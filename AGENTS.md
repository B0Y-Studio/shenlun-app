# AGENTS.md

给本项目工作的 AI agent（ZCode / Claude 等）阅读的执行约束。

## 强约束：禁止自行截图 / 识图

用户的文章/材料里可能含有敏感词。每次识图都可能被平台判定违规并处罚。

**因此 agent 必须遵守：**

- 不要主动调用 `android_screenshot` / `ios_screenshot`。
- 不要主动 Read 任何图片（`.png` / `.jpg` / `.jpeg` / `.webp` / `.gif` / `.bmp` / `.heic`）来"看"模拟器/真机界面。
- 调试 UI / 闪退 / 白屏 / 渲染问题时，**让用户自己截图**，由用户提供日志（logcat / 报错文本 / 复现步骤）。
- 写代码、改代码、跑命令、纯文本排错推理都不需要看图。
- 此规则优先级高于"看到问题先截图确认"的默认习惯。

如果用户主动贴了一张图，按用户给的来；不要因为图存在就主动再截一张去"对比"。

## 其他项目约定

- 看到 `android_screenshot` / `Read *.png` 的调用是 agent 自己发起的，视为违反本约束。